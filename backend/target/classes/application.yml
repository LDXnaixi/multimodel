server:
  port: 8080

spring:
  application:
    name: multimodal-test-backend

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    root: INFO
    com.changan.multimodal: INFO
